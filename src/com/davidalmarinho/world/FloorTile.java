package com.davidalmarinho.world;

import java.awt.image.BufferedImage;

public class FloorTile extends Tile {
    public FloorTile(int x, int y, BufferedImage tileSprite) {
        super(x, y, tileSprite);
    }
}
